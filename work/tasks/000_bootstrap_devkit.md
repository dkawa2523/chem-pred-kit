# Task 000: devkit（docs/work/codex/agentskills）導入

## 目的
- 大規模開発の前に「指示ブレを防ぐ仕組み」をリポジトリに導入する

## スコープ
- In scope:
  - `docs/`, `work/`, `codex/`, `agentskills/` を追加
  - README に開発フローを追記（最小）
- Out of scope:
  - コード本体の大改修（このタスクではやらない）

## Plan
1) これらディレクトリを repo ルートへ追加
2) `work/BACKLOG.md` に最初のタスク列挙
3) `agentskills/S00` を実行して現状マップを作る

## Acceptance Criteria
- [ ] devkit が repo に存在
- [ ] README から参照できる
- [ ] 次のタスクに着手できる状態
