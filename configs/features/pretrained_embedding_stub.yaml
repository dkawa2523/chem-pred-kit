featureset:
  name: "pretrained_embedding_stub"

featurizer:
  name: "pretrained_embedding"
  backend: "stub"
  embedding_dim: 256
  seed: 0
  normalize: true

data:
  cache_dir: "data/processed/cache/embedding"
