defaults:
  - process: visualize
  - hydra: default
  - _self_

input:
  evaluate_run_dir: "runs/evaluate/fp_evaluate"

plots:
  splits: ["val", "test"]
  include_train_hist: true
  target_mode: "primary"
  error_slicing:
    enabled: true

process:
  backend: "fp"

experiment:
  name: "fp_visualize"

output:
  exp_name: "fp_visualize"
