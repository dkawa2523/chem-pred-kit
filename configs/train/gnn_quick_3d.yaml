train:
  epochs: 10
  batch_size: 32
  lr: 0.001
  weight_decay: 1.0e-5
  loss: "huber"
  early_stopping:
    patience: 5
  seed: 42
  device: "auto"
  log_interval_sec: 10
  progress_bar: true
  max_batches_per_epoch: 50
