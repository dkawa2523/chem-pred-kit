train:
  epochs: 30
  batch_size: 128
  lr: 0.001
  weight_decay: 1.0e-5
  loss: "huber"
  early_stopping:
    patience: 10
  seed: 42
  log_interval_sec: 30
  progress_bar: true
  max_batches_per_epoch: 500
