defaults:
  - process: train
  - dataset: default
  - task: lj_epsilon
  - preprocess: none
  - features: gnn_graph
  - model: gnn_mpnn
  - pretrain: default
  - train: gnn_default
  - eval: default
  - hydra: default
  - _self_

experiment:
  name: "mpnn_desc_v1"

process:
  backend: "gnn"

output:
  run_dir: "runs/train/gnn"
  exp_name: "mpnn_desc_v1"
