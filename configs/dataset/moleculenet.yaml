paths:
  raw_csv: "data/raw/moleculenet/esol.csv"
  sdf_dir: "data/processed/moleculenet/esol/sdf"
  out_csv: "data/processed/moleculenet/esol/dataset.csv"
  out_indices_dir: "data/processed/moleculenet/esol/indices"

columns:
  sample_id: "sample_id"
  cas: "sample_id"
  smiles: "smiles"

dataset:
  name: "moleculenet"
  dataset_name: "esol"

moleculenet:
  root: "data/raw/moleculenet"
  allow_download: true
  add_hs: true
  compute_2d: true
  overwrite_sdf: false

seed: 42

split:
  method: "scaffold"
  seed: 42
  fractions: [0.8, 0.1, 0.1]

data:
  dataset_csv: "data/processed/moleculenet/esol/dataset.csv"
  indices_dir: "data/processed/moleculenet/esol/indices"
  sdf_dir: "data/processed/moleculenet/esol/sdf"
  cas_col: "sample_id"
