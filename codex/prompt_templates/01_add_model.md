# Prompt Template: Add Model

前提：
- codex/SESSION_CONTEXT.md
- agentskills/skills/S50_model_plugin.md
- work/tasks/<TASK_ID>_add_*.md

要求：
1) 変更計画（ファイル単位）を先に提示
2) registry と config を追加し `model.name=<new>` で選べるようにする
3) smoke test を追加
4) 検証コマンドを列挙
